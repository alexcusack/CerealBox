<div class="row">
  <div class="col-sm-8 col-sm-offset-2">
    <h2>Video form</h2>
    <%= form_for([@course, @course.videos.build],
      remote: true, data: { content: "video" }) do |f| %>
      <div class="form-group">
        <%= f.label :title %>:
        <%= f.text_field :title, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :link %>:
        <%= f.text_field :link, class: 'form-control' %>
      </div>

      <%= f.submit %>
    <%  end %>
  </div>
</div>


